cors:
  originPatterns: http://localhost:5173,http://localhost:8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sistema_oficina
    username: root
    password: password
  jooq:
    sql-dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  jpa:
    hibernate:
      ddl-auto: none
springdoc:
  paths-to-match: /api/**/**
  swagger-ui:
    use-root-path: true